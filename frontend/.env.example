# Dev server port (default 3600). Avoid 2911–3510 on Windows Hyper-V hosts.
PORT=3600

# Leave empty to use the Vite proxy (/api and /ws → backend :8000).
# Set only if you want the browser to call the API host directly.
# VITE_API_URL=http://localhost:8000
